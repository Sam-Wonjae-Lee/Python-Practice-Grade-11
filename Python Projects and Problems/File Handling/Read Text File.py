f = open("demofile.txt", "r")
print(f.read(5))

# The bracket between .read shows how many characters are read for the output
# Open function returns a file object, while the read function reads the content of the file
